{
  "name": "hyu-proxy-reg-from-lan",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "test": "python3 -m unittest discover -s tests -v",
    "check": "node --check app.js && python3 -m py_compile agent/agent.py && python3 -m unittest discover -s tests -v"
  }
}
